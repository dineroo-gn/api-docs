receiveOrderCreationEvent:
  options:
    tags:
      - Event Management
    operationId: receiveOrderEvent
    responses:
      "200":
        description: OK
        content:
          application/json:
            schema:
              $ref: "../components/schemas/OrderCreatedInfo.yaml"

orderAccepted:
  options:
    tags:
      - Event Management
    operationId: orderAccepted
    responses:
      "200":
        description: OK
        content:
          application/json:
            schema:
              $ref: "../components/schemas/OrderCreatedInfo.yaml"

orderRejected:
  options:
    tags:
      - Event Management
    operationId: orderRejected
    responses:
      "200":
        description: OK
        content:
          application/json:
            schema:
              $ref: "../components/schemas/OrderCreatedInfo.yaml"

orderPreparing:
  options:
    tags:
      - Event Management
    operationId: orderPreparing
    responses:
      "200":
        description: OK
        content:
          application/json:
            schema:
              $ref: "../components/schemas/OrderCreatedInfo.yaml"

deliveryCreated:
  options:
    tags:
      - Event Management
    operationId: deliveryCreated
    responses:
      "200":
        description: OK
        content:
          application/json:
            schema:
              $ref: "../components/schemas/DeliveryCreatedInfo.yaml"

driverAssigned:
  options:
    tags:
      - Event Management
    operationId: driverAssigned
    responses:
      "200":
        description: OK
        content:
          application/json:
            schema:
              $ref: "../components/schemas/DeliveryAssignedEvent.yaml"
