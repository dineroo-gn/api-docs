receiveOrderCreationEvent:
  options:
    tags:
      - Event Management
    operationId: receiveOrderEvent
    responses:
      "200":
        description: OK
        content:
          application/json:
            schema:
              $ref: "../components/schemas/OrderCreatedInfo.yaml"

orderStatus:
  options:
    tags:
      - Event Management
    operationId: orderStatus
    responses:
      "200":
        description: OK
        content:
          application/json:
            schema:
              $ref: "../components/schemas/OrderCreatedInfo.yaml"

deliveryCreated:
  options:
    tags:
      - Event Management
    operationId: deliveryCreated
    responses:
      "200":
        description: OK
        content:
          application/json:
            schema:
              $ref: "../components/schemas/DeliveryCreatedInfo.yaml"
