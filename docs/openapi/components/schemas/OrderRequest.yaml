type: object
required:
  - restaurantId
  - customerId
  - deliveryContact
  - orderItems
  - totalPrice
properties:
  restaurantId:
    type: string
    format: uuid
    totalPrice:
      type: number
      format: double
    deliveryContact:
      $ref: './DeliveryContactRequest.yaml'
    customerId:
      type: string
      format: uuid
  orderItems:
    type: array
    uniqueItems: true
    items:
      $ref: './OrderItemRequest.yaml'
  status:
    type: string
    example: PENDING
  comment:
    type: string
    maxLength: 300
    example: "Sans oignons svp"
