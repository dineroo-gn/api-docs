type: object
properties:
  userId:
    type: string
  status:
    type: string
    enum:
      - PENDING
      - APPROVED
      - REJECTED
  reason:
    type: string
  comment:
    type: string
required:
  - status
  - userId